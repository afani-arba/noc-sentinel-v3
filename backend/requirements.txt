# Core Framework
fastapi==0.110.1
uvicorn[standard]==0.25.0
python-multipart==0.0.22
python-dotenv==1.0.1

# Database
motor==3.3.1
pymongo==4.5.0

# Authentication
passlib[bcrypt]==1.7.4
bcrypt==4.1.3
PyJWT==2.8.0

# MikroTik API
RouterOS-api==0.17.0

# HTTP Client
httpx==0.27.0
requests==2.31.0

# SSH (untuk backup via SSH export)
paramiko==3.4.0
cryptography>=41.0.0

# Validation
pydantic==2.6.0
python-dateutil==2.8.2

# Server-Sent Events (real-time dashboard)
sse-starlette==2.1.3

# Time-Series Database (InfluxDB 2.x — opsional)
influxdb-client==1.50.0

# SNMP v2c (Hybrid Monitoring — traffic via SNMP 64-bit counters)
# pysnmp 7.x adalah nama baru (pysnmp-lextudio sudah deprecated, pakai pysnmp saja)
# import path baru: from pysnmp.hlapi.v3arch.sync import ... (via snmp_compat.py bridge)
pysnmp>=7.0.0
